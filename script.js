function carregar(){

    var inp_per = document.querySelectorAll('input.pericia')
    var qtd_per = inp_per.length()

        
    var teste = document.querySelector('div#teste')

    teste.style.color = 'green'
}